{
  "name": "volume-master-app",
  "version": "0.1.0",
  "description": "The desktop app for Volume master",
  "main": "app/main.js",
  "scripts": {
    "start": "electron app/.",
    "package": "electron-packager app/ --platform=darwin --arch=x64 --out=out --overwrite"
  },
  "devDependencies": {
    "electron-packager": "^14.2.1",
    "electron-prebuilt": "^1.2.7",
    "electron-prebuilt-compile": "^4.0.0"
  },
  "dependencies": {
    "auto-launch": "^5.0.5"
  }
}
