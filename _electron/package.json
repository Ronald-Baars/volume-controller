{
  "name": "volume-controller-app",
  "version": "0.1.0",
  "description": "The desktop app for Volume controller",
  "main": "app/main.js",
  "scripts": {
    "start": "electron app/.",
    "package": "electron-packager app/ --platform=darwin --arch=x64 --out=out --overwrite"
  },
  "devDependencies": {
    "electron-packager": "^7.3.0",
    "electron-prebuilt": "^1.2.7",
    "electron-prebuilt-compile": "^1.7.9"
  },
  "dependencies": {
    "auto-launch": "^5.0.5",
    "electron-reload": "^1.2.2"
  }
}
